SYNTHETIC fixtures for the VIXION 3.0 Increment 1 lab. Generated offline with the real
2.8 engine (`vixion_v28.PolicyState.on_tick` over `build_tick` ticks) from invented asks;
they are NOT production data and carry no statistical meaning. Used only to demonstrate
`scripts/v30_proposal_lab.py` replay/idempotency.
